<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="comm">

	<select id="getCommCtgCd" parameterType="hashmap" resultType="hashmap">
		SELECT COMM_CTG_NM FROM MAST001M ORDER BY SORT_NO
	</select>

	<select id="getCommCtgCdDtl" parameterType="hashmap" resultType="hashmap">
		SELECT COMM_CTG_DTL_NM FROM MAST001D WHERE WRK_MDUL_CD = #{WRK_MDUL_CD} AND COMM_CTG_CD = #{COMM_CTG_CD}
	</select>
	
</mapper>